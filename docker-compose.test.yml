# docker-compose.test.yml
# Ephemeral test stack (override for CI if needed).

services:
  api:
    environment:
      DATABASE_URL: sqlite+aiosqlite:///./data/test.db
      JWT_SECRET_KEY: test-secret-not-for-production
