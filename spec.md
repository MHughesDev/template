# Canonical specification (pointer)

The authoritative, versioned specification is **[`spec/spec.md`](spec/spec.md)**.

This repository root file keeps short links and bookmarks working when the full spec lives under `spec/`.
