---
doc_id: "21.2"
title: "authentication and authorization"
section: "Architecture"
status: "pending-init"
summary: "Auth strategy, token lifecycle, permission model, and which endpoints are public, protected, or admin-only."
---

# Authentication and Authorization
<!-- status: pending-init -->
<!-- initialized-by: skills/init/initialize-repo.md -->

> **Pending initialization.** This document is written by the `initialize-repo` skill.
> Run `make init:from-idea` to populate from `idea.md`.

## Purpose

Documents the auth strategy chosen for this project and why, how tokens are issued and refreshed, what the permission model looks like (roles, scopes, or resource-level), and the access level required for every API surface area.
