---
doc_id: "3.10"
title: "ci pipeline"
section: "Development"
status: "pending-init"
summary: "CI pipeline stages for this project, what each stage checks, the test matrix, and how to reproduce failures locally."
---

# CI Pipeline
<!-- status: pending-init -->
<!-- initialized-by: skills/init/initialize-repo.md -->

> **Pending initialization.** This document is written by the `initialize-repo` skill.
> Run `make init:from-idea` to populate from `idea.md`.

## Purpose

Documents the CI pipeline as configured for this specific project: every stage in order, what it checks, which profiles or matrix dimensions it runs against, how long it typically takes, and the exact local commands to reproduce any failure.
