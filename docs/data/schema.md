---
doc_id: "21.1"
title: "database schema"
section: "Data"
status: "pending-init"
summary: "Narrative description of the database schema: tables, their purpose, key relationships, and indexes with rationale."
---

# Database Schema
<!-- status: pending-init -->
<!-- initialized-by: skills/init/initialize-repo.md -->

> **Pending initialization.** This document is written by the `initialize-repo` skill.
> Run `make init:from-idea` to populate from `idea.md`.

## Purpose

Provides a human-readable narrative of the database schema: every table, what it stores and why, the relationships between tables, which indexes exist and what query patterns they serve. Companion to `docs/architecture/data-model.md` which defines the entity contracts; this doc describes the physical storage layer.
