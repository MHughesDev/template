# deploy/k8s/overlays/prod/kustomization.yaml
# BLUEPRINT: Composer 2 implements from this structure
# PURPOSE: Production environment Kustomize overlay. Full scale, strict settings, PDB. Per spec §26.10 item 254.
# bases: [../../base]
# namespace: production
# patches:
#   - replicas=3, resources.limits={cpu: 1000m, memory: 1Gi}, resources.requests={cpu: 250m, memory: 256Mi}
#   - LOG_LEVEL=INFO, LOG_FORMAT=json, API_DEBUG=false
#   - host=api.{{DOMAIN}}
#   - HPA minReplicas=3, maxReplicas=20
#   - Add PodDisruptionBudget: minAvailable=2
# images: [{name: ghcr.io/{{ORG}}/{{REPO}}, newTag: stable}]
