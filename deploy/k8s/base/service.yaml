# deploy/k8s/base/service.yaml
# BLUEPRINT: Composer 2 implements from this structure
# PURPOSE: Base Kubernetes Service exposing the API deployment. Per spec §26.10 item 249.
# apiVersion: v1, kind: Service
# spec: ClusterIP (internal), selector: app=api, ports: [name:http, port:80, targetPort:8000]
