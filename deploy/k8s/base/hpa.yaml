# deploy/k8s/base/hpa.yaml
# BLUEPRINT: Composer 2 implements from this structure
# PURPOSE: HorizontalPodAutoscaler. Patched per overlay for scaling thresholds. Per spec §26.12 item 367.
# apiVersion: autoscaling/v2, kind: HorizontalPodAutoscaler
# spec: scaleTargetRef: {kind: Deployment, name: api}, minReplicas: 1, maxReplicas: 10
# metrics: CPU (80%), Memory (80%)
