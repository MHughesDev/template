# deploy/k8s/base/kustomization.yaml
# BLUEPRINT: Composer 2 implements from this structure
# PURPOSE: Kustomize base configuration. References all base resources. Per spec §26.10 item 251.
# apiVersion: kustomize.config.k8s.io/v1beta1, kind: Kustomization
# resources: [deployment.yaml, service.yaml, configmap.yaml, hpa.yaml, ingress.yaml, networkpolicy.yaml, serviceaccount.yaml]
# commonLabels: app: api, managed-by: kustomize
