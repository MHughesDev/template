# deploy/k8s/base/configmap.yaml
# BLUEPRINT: Composer 2 implements from this structure
# PURPOSE: Base ConfigMap for non-secret configuration values. Per spec §26.10 item 250.
# apiVersion: v1, kind: ConfigMap
# data: API_HOST: "0.0.0.0", API_PORT: "8000", LOG_LEVEL: "INFO", LOG_FORMAT: "json"
# NOTES: Secrets (DATABASE_URL, JWT_SECRET_KEY) are in a Kubernetes Secret, not here
