# .github/workflows/label-sync.yml
# BLUEPRINT: Composer 2 implements from this structure
# PURPOSE: Sync GitHub labels from .github/labels.yml. Optional per spec §26.12 item 395.
# SECTIONS:
#   - on: push (paths: [".github/labels.yml"])
#   - uses: EndBug/label-sync action to sync labels from labels.yml to repo
name: Label Sync
# on: push: paths: [".github/labels.yml"]
# jobs:
#   label-sync:
#     steps:
#       - checkout
#       - uses: EndBug/label-sync with labels-from: .github/labels.yml, delete-other-labels: false
