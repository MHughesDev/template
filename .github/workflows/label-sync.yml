# .github/workflows/label-sync.yml
name: Labels

on:
  workflow_dispatch: {}

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Note
        run: |
          echo "Label definitions live in .github/labels.yml — apply manually or via GitHub UI."
