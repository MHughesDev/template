# .github/workflows/security.yml
name: Security

on:
  pull_request:
    branches: [main]
  schedule:
    - cron: "0 12 * * 1"
  workflow_dispatch: {}

jobs:
  dependency-review:
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      # Requires Dependency graph (repo Settings → Code security). Do not block CI if disabled.
      - uses: actions/dependency-review-action@v4
        continue-on-error: true

  code-scan:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/setup-python
      - run: make security-scan

  image-scan:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: make image-build
      - name: Trivy scan
        # v0.33.1 defaulted to Trivy v0.65.0 binary; setup occasionally failed. v0.35.0 defaults to v0.69.3.
        uses: aquasecurity/trivy-action@v0.35.0
        with:
          image-ref: template-api:local
          version: v0.69.3
          severity: CRITICAL,HIGH
          exit-code: "1"
          trivy-config: trivy.yaml
          trivyignores: .trivyignore
