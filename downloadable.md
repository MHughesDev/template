# Specification location

The **canonical** template repository specification is **[`spec.md`](./spec.md)** (version 2.0 — agent-operated machine).

`downloadable.md` is retained only for **backward compatibility** with older links. All content lives in `spec.md`.
